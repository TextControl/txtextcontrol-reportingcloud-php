inherit: true

checks:
    php:
        code_rating: true
        duplication: true

filter:
    paths:
    - src/*

build:
    nodes:
        php56:
            environment:
                php: '5.6.16'
            tests:
                override:
                    -
                        command: composer test
                        coverage:
                            file: build/phpunit/clover.xml
                            format: php-clover
